# Intake YAML

Copy a file here, replace values, paste into Cursor. Do not invent people, dates, hours, or REQ IDs.

Optional: `python3 scripts/validate_intake.py inputs/examples/plan.cycle-59.yaml`

Goldens: `docs/`. Fake data only.
